package card

import(
	"net/http"
	. "library"
)

func Createcard(w http.ResponseWriter, r *http.Request) {
	material := make(map[string]string)
	Output(w, "page/cardset/card/createcard.html", material)
}
